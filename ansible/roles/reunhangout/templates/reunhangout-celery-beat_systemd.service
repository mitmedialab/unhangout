[Unit]
Description=Reunhangout celery daemon 

[Service]
Type=simple
User=www-data
Group=www-data
ExecStart {{django_venv_dir}}bin/celery -A {{django_project_name}} beat -l info --workdir={{django_project_dir}}{{django_project_name}}

[Install]
WantedBy=multi-user.target
